## 回顾

首次为 harness CLI 引入遥测能力。采用 fire-and-forget + Hono 轻量服务的方案，实现成本低、对 CLI 用户无感。

### 做得好的
- 零新 CLI runtime 依赖，利用 Node 20 原生 fetch
- 服务端与静态托管合并，减少部署复杂度
- Git 信息采集使项目归属可追踪

### 可改进的
- 端点地址硬编码为 localhost，部署时需通过环境变量覆盖
- JSONL 文件存储在大数据量下可能需要轮转或迁移到数据库
- 统计面板无时间范围筛选，后续可增强
