---
name: "OPSX: Fast Forward"
description: Explicitly fast-forward a change without bypassing design confirmation
category: Workflow
tags: [workflow, artifacts, experimental]
---

Load and follow the `openspec-ff-change` skill.

Pass the raw request, optional schema choice, and relevant hot conversation context. Return the one
blocking decision or projected artifacts with compact decision provenance and the apply prompt.
